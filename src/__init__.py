"""Telegram Agent Framework."""

# Single source of truth for the package version.
# `pyproject.toml` reads this via [tool.setuptools.dynamic], and
# tests/unit/test_version.py asserts the CHANGELOG agrees with it.
__version__ = "2.1.0"
